# プライバシーポリシー

Link Radar for X（以下「本拡張」）は、X（Twitter）に表示されたリンクを分類して表示することのみを
目的としています。

## 要約

- 収集した情報を**外部サーバーへ送信しません**。判定はすべて利用者のブラウザ内で完結します。
- 広告・トラッキング・アナリティクスは**一切行いません**。
- アカウント登録も、開発者への情報提供もありません。

## 処理する情報

- **画面内に表示されたリンクのURLと表示テキスト。** 判定のためにローカルで処理します。
- **そのリンク先のページ内容。** 有料記事か / 広告枠がいくつあるか / 登録が必要か / 短縮URLの
  行き先はどこか を判定するために、拡張機能のバックグラウンドで取得（fetch）します。
  取得はCookieを送らない匿名リクエスト（`credentials: "omit"`）です。
- 短縮リンク（`t.co` 等）の行き先を確認するため、中継ページを取得することがあります。

処理するのは**Xの画面に実際に表示されたリンクだけ**です。閲覧履歴、他のタブの内容、
フォーム入力、Cookie、認証情報には一切アクセスしません。

## 権限が必要な理由

| 権限 | 用途 |
|---|---|
| `storage` | 判定結果のキャッシュと設定の保存 |
| `<all_urls>` | 上記「リンク先のページ内容」の取得 |

`<all_urls>`（すべてのサイトの読み取り）は広い権限ですが、本拡張がこれを使うのは
**Xの画面に出たリンク先を取得する**ためだけです。判定対象のドメインを事前に列挙することは
できない（誰がどのサイトのリンクを貼るか分からない）ため、この形を取っています。

この動作が不要な場合は、拡張機能のポップアップで「リンク先を取得して判定する」をOFFにできます。
OFFにすると、リンク先の取得を一切行わなくなります（URLだけで判定できるアダルト/アフィリンク/
短縮URL/まとめサイト/連投の検出は引き続き動作します）。

## 保存

- 判定結果を高速化のため `chrome.storage.local` に**最大6時間**キャッシュします。
  閲覧履歴として蓄積することはありません。
- 設定（カテゴリのON/OFF等）は `chrome.storage.sync` に保存されます。
  Chromeにログインしている場合、Chromeの機能によって端末間で同期されます。

## 第三者提供

ありません。取得先はリンク先サイトのサーバーのみで、閲覧内容を第三者と共有しません。

## ソースコード

本拡張は MIT ライセンスのオープンソースです。上記の内容はソースコードで検証できます。
ネットワークアクセスは `src/background.js` の `fetchWithBody()` に集約されています。

## 連絡先

本拡張に関するお問い合わせは、GitHubリポジトリの Issues からお願いします。

## 更新日

2026-07-26
