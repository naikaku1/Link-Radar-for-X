{
  "name": "link-radar-for-x",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Xのリンクを開く前にアダルト/連投スパム/有料記事/広告過多などに分類するChrome拡張",
  "scripts": {
    "test": "node test/test.js && node test/test-background.js",
    "pack": "rm -f link-radar.zip && zip -rq link-radar.zip manifest.json src icons -x '*.DS_Store' && unzip -l link-radar.zip"
  },
  "license": "MIT",
  "author": "Naikaku"
}
